<h1>ユーザー詳細</h1>

<div>
<p>ユーザー名：<%= @user.name %></p>
<p>メールアドレス：<%= @user.email %></p>
<p>パスワード：<%= @user.password %></p>
</div>
<div>
<% if logged_in? && @user == current_user %>
    <%= link_to "編集", edit_user_path %>
    <%= link_to "削除", user_path(@user),  method: :delete, data: { turbo_method: :delete, turbo_confirm: '本当に削除しますか？' } %>
<% end %>
</div>