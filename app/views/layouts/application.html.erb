<!DOCTYPE html>
<html>
  <head>
    <title>AttendancesController</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags "application" %>
  </head>

  <body>

    <header class="mb-5"><nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
  <%= link_to "勤怠管理", root_path, class: 'navbar-brand' %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
              <li class="nav-item"><%= link_to "TOP", root_path, class: 'nav-link' %></li>
              <li class="nav-item"><%= link_to "勤怠状況", works_path, class: 'nav-link' %></li>
              <% if logged_in? %>
              <li class="nav-item"><%= link_to "ログアウト", logout_path, method: :delete, class: 'nav-link', data: { turbo_method: :delete, turbo_confirm: " 本当にログアウトしますか？"} %></li>
              <li class="nav-item"><%= link_to "アカウント設定", user_path(current_user), class: 'nav-link' %></li>
              <% else %>
              <li class="nav-item"><%= link_to "ログイン", login_path, class: 'nav-link' %></li>
              <li class="nav-item"><%= link_to "新規登録", signup_path, class: 'nav-link' %></li>
              <% end %>
          </ul>
    </div>
  </div>
</nav>
    </header>

    <main>

    <% flash.each do |key, message| %>
    <div id="flash-message">
      <% if message.is_a?(Array) %>
        <% message.each do |msg| %>
          <p class="<%= key %>"><%= msg %></p>
        <% end %>
      <% else %>
          <p class="<%= key %>"><%= message %></p>
      <% end %>
      </div>
    <% end %>
    <section class="container">
    <%= yield %>
    </section>
    </main>
  </body>
</html>
